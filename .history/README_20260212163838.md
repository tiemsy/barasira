# Barasira

Barasira est une plateforme de mise en relation entre clients et prestataires de services, construite avec Laravel 12. Elle expose une API REST (protégée par Sanctum) et un front moderne (Vue/Inertia).

## Sommaire
- Présentation
- Architecture
- Tables principales
- Tables CVThèque
- Démarrage rapide
- Variables d’environnement
- Rôles utilisateurs
- Commandes utiles
- Documentation API
- API REST
- Modules principaux
- Flux “Mission” recommandé
- Frontend
- Qualité, tests et style
- Sécurité
- Déploiement
- Bonnes pratiques

## Présentation
- Objectif: permettre aux clients de publier des missions et aux prestataires d’y postuler, exécuter, puis recevoir une évaluation.
- Domaines couverts: utilisateurs, services, catégories, missions, candidatures, avis.

## Architecture
- Backend: Laravel (PHP 8.2+)
- Authentification: Laravel Sanctum (cookies ou tokens)
- Front: Vue 3 + Inertia.js (selon configuration du repo)
- Build: Vite
- Documentation API: L5-Swagger (génération) + openapi.yaml minimal (manuel) à la racine

## Tables principales (12 tables métier)
1. users - Utilisateurs (clients, prestataires, admins)
2. services - Types de services proposés
3. missions - Missions créées par les clients
4. applications - Candidatures des prestataires
5. payments - Paiements / transactions
6. reviews - Avis et notes
7. messages - Messagerie interne
8. user_skills - Compétences / tarifs
9. notifications - Notifications utilisateurs
10. documents - Documents d'identité / justificatifs
11. disputes - Litiges entre utilisateurs
12. favorites - Prestataires favoris

## Tables CVThèque
- resumes - CV des utilisateurs
- experiences - Expériences professionnelles
- educations - Formations
- certifications - Certifications
- resume_languages - Langues parlées
- resume_tags - Tags de compétences
- portfolio_items - Éléments de portfolio

## Démarrage rapide en local
# bash
1) Dépendances
composer install
npm install
npm install --legacy-peer-deps // pour Docker


2) Env et clé
cp .env.example .env
php artisan key:generate

3) Base de données
php artisan migrate --seed

4) Serveurs dev
php artisan serve
npm run dev

(optionnel) Mail local
mailpit

## Variables d’environnement
- DB_*: connexion base de données
- SANCTUM_STATEFUL_DOMAINS, SESSION_DOMAIN: pour SPA/CORS
- APP_URL, FRONTEND_URL: liens applicatifs
- MAIL_*: envoi d’e-mails (sandbox en dev)

## Rôles utilisateurs
- client — Utilisateur standard (défaut)
- prestataire — Fournisseur de services
- admin — Administrateur
- superadmin — Super administrateur

## Commandes utiles
# bash
Vérifier les routes API
php artisan route:list --path=api

Générer la doc Swagger (L5-Swagger)
php artisan l5-swagger:generate

npm run dev # Serveur de développement Vite
npm run sass # Compilation Sass
npm run watch:sass # Watch Sass
php artisan serve # Serveur Laravel
maildev # Serveur mail local

Cache/optimize (prod)
php artisan config:cache
php artisan route:cache
php artisan view:cache

## Documentation API
- Spécification générée (L5-Swagger): storage/api-docs/api-docs.yaml
- Spec minimal manuelle: openapi.yaml (à la racine)
- Routes principales (routes/api.php):
  - Auth: POST /api/login, POST /api/register, POST /api/logout, GET /api/me
  - Ressources: users, user-skills, service-categories, services (+ GET /api/services-search), missions, reviews, resumes, portfolio-items

## API REST
- Routes définies dans routes/api.php.
- Controllers dans app/Http/Controllers/Api/.
- Utiliser Route::apiResource pour CRUD.
- Authentification: Sanctum (cookies ou tokens).
- Endpoints d’auth:
  - POST /api/login (middleware web + guest)
  - POST /api/register (middleware web)
  - POST /api/logout (auth:sanctum)
  - GET /api/me


## Modules principaux
- Users, Profiles (skills, resumes, portfolio)
- Services & Categories
- Missions (création par clients)
- Reviews (avis/notations)
- Recherche de services (services-search)

## Flux “Mission” recommandé
1) Client crée une mission (POST /api/missions → status: pending)
2) Prestataire postule (endpoint applications si présent, sinon action dédiée)
3) Client accepte une candidature (mission → in_progress)
4) Prestataire exécute et clôture (PATCH /api/missions/{id} → completed)
5) Client dépose un avis (POST /api/reviews)

États:
- Mission: pending → in_progress → completed (→ cancelled si besoin)

## Frontend
- Vue 3 (Composition API) + Inertia.js.
- Bootstrap 5.3.8 pour le style + SASS pour la personnalisation.
- Vite pour le build et le HMR.

## Qualité, tests et style
- Tests: PHPUnit
- Style: Laravel Pint (recommandé)
- Debug: Laravel Debugbar (dev), Ignition pour erreurs
- Recommandations:
  - FormRequest pour la validation
  - Policies pour l’accès (missions, reviews…)
  - Resources (JsonResource) pour les réponses
  - Pagination par défaut

## Sécurité
- Sanctum pour l’authentification
- Vérification email (MustVerifyEmail)
- Soft deletes pour la traçabilité
- Validation stricte des entrées (FormRequest)
- Gestion des documents d’identité
- Système de disputes

## Déploiement
- Build production avec Vite
- Assets versionnés
- Option Docker (Laravel Sail) si besoin
- Variables d’environnement documentées dans .env.example

## Bonnes pratiques

- Valider toutes les requests (FormRequest + messages d’erreur localisés).
- Utiliser Resources (JsonResource) pour les réponses API.
- Paginer les listings (par défaut 15–20).
- Protéger les routes critiques avec policies/gates.
- Éviter la logique métier dans les controllers: Services/Actions/Jobs.
- Journaliser les transitions de statuts (events + listeners).
- Tests d’intégration pour les flux critiques (missions, applications, paiements).
